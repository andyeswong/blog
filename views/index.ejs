<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AWONG_blog</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/styles.css">
</head>
<body class="bg-black text-white font-mono overflow-x-hidden">
  <!-- Hero Section -->
  <section class="hero-main min-h-screen w-full flex flex-col justify-center items-center relative overflow-hidden"  style="background-color: var(--body-bg)">

    <div class="relative z-10 w-full flex flex-col items-center justify-center">
      <!-- Navigation Menu - Dynamic Island Style -->
      <nav class="fixed top-6 left-1/2 transform -translate-x-1/2 z-50 mb-20">
        <div class="flex items-center justify-center gap-4 px-6 py-3 rounded-lg w-100 transition-all duration-500">
          <span class="text-sm font-light tracking-[0.1em]" >AWONG_blog</span>
          <div class="w-px h-6" style="background-color: var(--border-color)"></div>
          <div class="flex gap-1">
            <a href="/" class="px-6 py-2 mx-1 font-light tracking-[0.1em] transition-all duration-300 rounded-lg border" >inicio</a>
            <a href="/posts" class="px-6 py-2 mx-1 font-light tracking-[0.1em] transition-all duration-300 rounded-lg hover:bg-opacity-50" style="color: var(--purple)">artículos</a>
            <a href="/about" class="px-6 py-2 mx-1 font-light tracking-[0.1em] transition-all duration-300 rounded-lg hover:bg-opacity-50" style="color: var(--purple)">acerca</a>
          </div>
          <div class="w-px h-6" style="background-color: var(--border-color)"></div>
          <button id="theme-toggle" class="theme-toggle" title="Cambiar tema"></button>
        </div>
      </nav>

          <div class="max-w-3xl mx-auto">
      <!-- Section Header -->
      <div class="text-center mb-12">
        <h2 class="text-2xl md:text-3xl font-light tracking-tight mb-4" style="color: var(--fg)">
          ¿Qué te interesa aprender hoy?
        </h2>
        <p class="text-sm font-light opacity-70" style="color: var(--comment)">
          Describe lo que buscas y te recomendaré artículos relevantes
        </p>
      </div>

      <!-- Recommendation Form -->
      <div class="recommendation-container">
        <form id="recommend-form" class="mb-8">
          <div class="relative">
            <textarea
              id="recommend-input"
              placeholder="Ej: Quiero aprender sobre inteligencia artificial y cómo integrarla en mis proyectos..."
              class="w-full px-6 py-4 rounded-lg font-mono font-light text-sm resize-none transition-all duration-300 focus:outline-none"
              style="background-color: var(--current-line); color: var(--fg); border: 1px solid var(--border-color); min-height: 120px;"
              rows="4"
            ></textarea>
            <button
              type="submit"
              id="recommend-btn"
              class="absolute bottom-4 right-4 px-4 py-2 rounded-lg font-mono font-light text-sm transition-all duration-300 hover:opacity-80"
              style="background-color: var(--purple); color: var(--bg)"
            >
              <span id="recommend-btn-text">Recomendar</span>
              <span id="recommend-btn-loading" class="hidden">...</span>
            </button>
          </div>
        </form>

        <!-- Results Container -->
        <div id="recommend-results" class="hidden">
          <div class="p-6 rounded-lg" style="background-color: var(--current-line); border: 1px solid var(--border-color)">
            <div class="flex items-start gap-3 mb-4">
              <span class="text-lg">🤖</span>
              <h3 class="font-mono font-light text-sm" style="color: var(--purple)">Recomendaciones</h3>
            </div>
            <div id="recommend-content" class="font-mono font-light text-sm leading-relaxed markdown-content" style="color: var(--fg)">
              <!-- AI response will be inserted here -->
            </div>
          </div>
          <p class="text-center mt-4 text-xs font-light opacity-50" style="color: var(--comment)">
            AI puede cometer errores. Verifica las respuestas importantes.
          </p>
        </div>

        <!-- Error Container -->
        <div id="recommend-error" class="hidden">
          <div class="p-4 rounded-lg text-center" style="background-color: rgba(255, 85, 85, 0.1); border: 1px solid var(--red)">
            <p class="font-mono font-light text-sm" style="color: var(--red)" id="recommend-error-text">
              Error al obtener recomendaciones
            </p>
          </div>
        </div>
      </div>
    </div>

    </div>

    <!-- Scroll indicator -->
   
  </section>

  <%- include('footer') %>

  <div id="mouse-gradient" class="fixed pointer-events-none w-96 h-96 rounded-full blur-3xl transition-all duration-500 ease-out opacity-0" style="background: radial-gradient(circle, rgba(189, 147, 249, 0.05) 0%, transparent 100%)"></div>

  <script src="/theme.js"></script>
  <script src="/glitch.js"></script>
  <script src="/recommend.js"></script>
</body>
</html>
